# PCA From Scratch
Find principal directions with covariance and eigenvectors. Run `python main.py`; chart: `output/pca_projection.png`.

Open [`03_pca.ipynb`](03_pca.ipynb) to read the explanation and see the embedded visualization immediately.
