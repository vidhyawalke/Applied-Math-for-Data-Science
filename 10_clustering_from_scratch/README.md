# Clustering From Scratch
Assign points to their closest center and recompute group means with k-means. Run `python main.py`; chart: `output/kmeans_clusters.png`.

Open [`10_clustering_from_scratch.ipynb`](10_clustering_from_scratch.ipynb) to read the explanation and see the embedded visualization immediately.
