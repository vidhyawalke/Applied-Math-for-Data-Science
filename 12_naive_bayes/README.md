# Naive Bayes From Scratch
Use word counts and Laplace smoothing to classify messages. Run `python main.py`; chart: `output/message_predictions.png`.

Open [`12_naive_bayes.ipynb`](12_naive_bayes.ipynb) to read the explanation and see the embedded visualization immediately.
