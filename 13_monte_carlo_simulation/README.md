# Monte Carlo Simulation Lab
Estimate pi by sampling points in a square around a quarter circle. Run `python main.py`; chart: `output/pi_estimate.png`.

Open [`13_monte_carlo_simulation.ipynb`](13_monte_carlo_simulation.ipynb) to read the explanation and see the embedded visualization immediately.
