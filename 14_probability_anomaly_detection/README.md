# Anomaly Detection Using Probability
Calculate z-scores and flag values more than two standard deviations from the mean. Run `python main.py`; chart: `output/z_score_anomalies.png`.

Open [`14_probability_anomaly_detection.ipynb`](14_probability_anomaly_detection.ipynb) to read the explanation and see the embedded visualization immediately.
